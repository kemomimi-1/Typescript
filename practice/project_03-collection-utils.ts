// ==============================================
// 项目练习 03：列表工具库（泛型 + 函数类型）
// 目标：练习泛型函数、函数类型签名设计
// ==============================================

/**
 * 任务 1：filterBy
 *
 * 需求：
 * - 根据条件过滤数组
 * - 输入：一个数组 + 一个判断函数
 * - 输出：过滤后的数组
 *
 * 建议：
 * - 使用泛型 T 表示数组元素类型
 * - 判断函数签名类似：(item: T) => boolean
 */

// TODO: 设计并实现 filterBy 的函数签名和函数体
// function filterBy(/* 在这里写上泛型和参数类型 */) {
//   // 使用数组的 filter 方法完成过滤
// }

/**
 * 任务 2：groupBy
 *
 * 需求：
 * - 根据 key 函数对数组进行分组
 * - 输入：一个数组 + 一个从元素计算 key 的函数
 * - 输出：以 key 为属性、值为该 key 对应元素数组的对象
 *
 * 提示：
 * - 可以使用两个泛型：T 表示元素类型，K 表示 key 类型
 * - K 通常需要受限为 string | number | symbol
 * - 返回类型可以用 Record<K, T[]>
 */

// TODO: 设计并实现 groupBy 的函数签名和函数体
// function groupBy(/* 在这里写上泛型和参数类型 */) {
//   // 遍历数组，根据 getKey(item) 进行分组
// }

/**
 * 任务 3：sortBy
 *
 * 需求：
 * - 根据对象的某个字段进行排序
 * - 输入：一个数组 + 一个字段名
 * - 输出：按该字段升序排序后的“新数组”（不要修改原数组）
 *
 * 提示：
 * - 使用泛型 T，和类型参数 K extends keyof T 表示字段名
 * - 返回类型仍然是 T[]
 * - 可以用 [...items] 复制一份再排序
 */

// TODO: 设计并实现 sortBy 的函数签名和函数体
// function sortBy(/* 在这里写上泛型和参数类型 */) {
//   // 复制数组并根据 key 对元素排序
// }

/**
 * 建议测试思路：
 * - 使用你在其他练习中定义的 User 接口：
 *   - 用 filterBy 过滤 isActive === true 的用户
 *   - 用 groupBy 按 department 分组
 *   - 用 sortBy 按 age 或 username 排序
 */
